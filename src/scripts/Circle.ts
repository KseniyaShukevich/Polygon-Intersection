export default class Circle {
    r: number
    startAngle: number
    endAngle: number

    constructor(
        r: number, 
        startAngle: number, 
        endAngle: number
        ) {
            this.r = r,
            this.startAngle = startAngle,
            this.endAngle = endAngle
    }
}